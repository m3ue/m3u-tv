# sqflite_tvos — porting report

Generated by `flutter-tvos plugin port` on 2026-05-18.

Source: `sqflite_darwin` 2.4.2 (path: `/Users/aliustaoglu/.pub-cache/hosted/pub.dev/sqflite_darwin-2.4.2`)
Base platform: ios (Objective-C)
Output: `./sqflite_tvos`

> ✅ No tvOS-incompatible APIs detected at type level — the generated package is expected to compile on tvOS (still review stubbed/partial items below).

## Summary

| Status | Count |
|---|---|
| Methods ported as-is | 0 |
| Methods stubbed (iOS-only) | 0 |
| Native regions disabled on tvOS | 0 |
| tvOS build outlook | ✅ expected to compile |
| Manual review items | 0 |

## Methods

No `case "<method>":` handlers were detected in the source. Either the plugin dispatches method calls in a non-standard way (review `tvos/Classes/` by hand) or it has no method channel.

## Imports removed

None. Every `import` in the source compiles on tvOS.

## Disabled on tvOS

None. No type-level tvOS-incompatible API was found; nothing had to be compiled out.

## Manual review items

None flagged automatically. You should still skim `tvos/Classes/` — regex-based porting is best-effort and cannot catch every obfuscated API use.

## Checklist

- [x] Read every `✗ stubbed` method above and confirm returning `FlutterMethodNotImplemented` is acceptable on tvOS.
- [ ] Review every `⚠️ partial` method against a real Apple TV (behaviour differs from iOS). — simulator-verified; on-device pass still recommended
- [x] Confirm the removed imports were not load-bearing for still-supported code paths.
- [x] `flutter-tvos build tvos --simulator --debug` from the plugin's example app compiles the generated registrant.
- [x] Bump the version and update `CHANGELOG.md` before publishing.

---

Manual review required. Read this report top-to-bottom before publishing `sqflite_tvos`.
