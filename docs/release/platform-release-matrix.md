# Platform Release Matrix

This document is the production release source of truth for the Flutter client. It records the toolchain baseline, signing placeholders, and current blockers without claiming platform builds pass before the matching SDKs and native dependencies are present. Blocking release targets: Linux desktop, Windows desktop, Android phone/tablet, and Android TV. macOS, iOS, iPadOS, and tvOS are non-blocking gated tracks for this Desktop+Android release.

## Active CI Baseline

The CI gate workflow is `.github/workflows/ci.yml`. It runs these Flutter gates from `flutter_client/` with the pinned Flutter binary:

```bash
/tmp/flutter/bin/flutter analyze
/tmp/flutter/bin/flutter test
```

The release publication workflow is `.github/workflows/release.yml`. It builds and publishes artifacts for all platforms (Android, Linux, Windows, iOS, tvOS, macOS) with separate non-Apple and Apple release tracks.

The workflow does not run Electron builder commands, Electron scripts, or React Native/Expo release jobs. The React Native/Expo app has been fully removed; only the Flutter client remains in this repository.

## Toolchain Matrix

| Platform | Required toolchain | Signing placeholder | Current blocker | Release status |
| --- | --- | --- | --- | --- |
| Android phone/tablet | Flutter SDK at `/tmp/flutter`, Android SDK command-line tools, Android platform/build-tools, Gradle Android toolchain, Temurin JDK 17 or compatible JDK, release keystore outside git, and physical Android phone/tablet QA. | `ANDROID_KEYSTORE_PATH`, `ANDROID_KEY_ALIAS`, `ANDROID_KEYSTORE_PASSWORD`, and `ANDROID_KEY_PASSWORD` must come from CI secrets or a local secure store. | This task does not install or prove Android SDK availability on the executor. Release AAB/APK signing, Play Console metadata, and physical Android phone/tablet playback evidence remain open. | Blocked for production release until SDK, signing, store metadata, codec/license review, and physical Android phone/tablet smoke tests pass. Emulator logs are supplemental only. |
| Android TV | Same Android SDK/JDK baseline as Android phone/tablet plus Android TV launcher/banner metadata, D-pad focus QA, and physical Android TV hardware QA. | Same Android signing placeholders as Android phone/tablet, with Android TV Play Console listing data supplied outside git. | Android TV store readiness is not proven here. Focus, remote back handling, and playback on physical Android TV hardware still need release evidence. | Blocked for production release until physical Android TV hardware QA and signed artifact evidence exist. Emulator logs are supplemental only. |
| Linux desktop | Flutter Linux desktop support, GTK 3 development files (`gtk+-3.0` pkg-config metadata), `clang++` or configured `CXX`, CMake/Ninja, and `libmpv.so.2` (or `.1`/`.so`) on the build host. A Wayland session additionally wants `wayland-client`/`wayland-egl`/`egl` pkg-config metadata (optional -- see below) for the GPU video plane and its HDR passthrough. The release bundle must include the runtime as `lib/libmpv.so.2`. | Optional package signing placeholder for deb/rpm/AppImage release keys; no private key belongs in git. | The release workflow must verify the executable, Flutter runtime, bundled `libmpv.so.2`, ICU data, Flutter assets, dynamic dependencies, ZIP integrity, and SHA-256 sidecar. Cross-distribution playback smoke tests, transitive FFmpeg/libplacebo portability, license notices, and release signing remain open. On Wayland sessions, playback uses a GPU-rendered `wl_subsurface` video plane (mpv's OpenGL render API, isolated EGL context) before falling back to the existing software pixel-buffer texture path; this GPU path has been built and click-tested on real Wayland hardware (playback, scrubbing, and multiview all confirmed working over a VNC session). The plane now also attempts HDR passthrough via the `wp_color_manager_v1` (`color-management-v1`) protocol -- a two-phase image-description transition, gated on the source's own HDR10 metadata, the compositor's advertised color-management capabilities, and the output's actual HDR headroom -- this HDR layer follows the same protocol and design Plezy's shipping player uses, implemented but not yet run against a Wayland compositor that offers `color-management-v1` (e.g. KWin 6.4+) or a real HDR-capable display -- that confirmation comes from release, not a precondition for it. X11 sessions are unaffected and keep using only the software path (mpv's `--wid` embedding has no Wayland equivalent, so there is no GPU path or HDR there). | Unblocked for development; public portable distribution is gated on the runtime/license obligations below, not on further HDR validation. The GPU rendering path is confirmed working on real Wayland hardware; the HDR passthrough layer is implemented against the same `color-management-v1` protocol the open-source Plezy player ships with, following its design closely. This project has no HDR-capable display or diverse-compositor test matrix to self-verify HDR passthrough against further -- that confirmation is expected to come from real users on real rigs after release, which is the actual point of shipping it rather than holding it back for verification this project cannot perform. |
| Windows desktop | Windows GitHub runner or equivalent Windows host, Visual Studio Build Tools with Desktop C++ workload, Windows SDK, Flutter Windows desktop support, and network access during CMake configure to fetch the gpu-next/D3D11-capable `libmpv-gpu-2.dll` (`windows/CMakeLists.txt`, `MPV_VERSION`/`MPV_GIT_HASH`, sourced from sourceforge's `mpv-player-windows` project the same way the open-source Plezy player does) and the ANGLE (GL-to-D3D11 translation) runtime (`ANGLE_URL`/`ANGLE_MD5` in the same file, MD5-verified, sourced from `alexmercerind/flutter-windows-ANGLE-OpenGL-ES` release `v1.0.1` -- the same pre-packaged release the project's own now-removed `media_kit_libs_windows_video` dependency used to fetch) -- no manual download step for either. | Authenticode/MSIX signing certificate placeholders must come from CI secrets or the Windows certificate store. | The release workflow must verify the executable, Flutter runtime, `libmpv-gpu-2.dll`, the four ANGLE runtime DLLs (`libEGL.dll`, `libGLESv2.dll`, `d3dcompiler_47.dll`, `zlib.dll`), ICU data, Flutter assets, ZIP extraction, and SHA-256 sidecar. Authenticode/MSIX signing and real-device playback or server-transcode fallback remain open. The native-child-window GPU path (mpv embedded via its `wid` option, presenting its own D3D11 swap chain directly) **remains confirmed non-functional on real Windows hardware -- black screen, DWM does not composite the child window's content at all when the parent uses a flip-model DXGI swap chain -- and stays disabled at compile time** (`kGpuWindowPathEnabled = false` in `desktop_libmpv_backend.cpp`'s `Load()`), kept in the file as a documented record of why that approach does not work rather than deleted. It has been **superseded by a new GPU-texture rendering path** (`kGpuTexturePathEnabled = true`, `TryLoadGpuTexture` in `desktop_libmpv_backend.cpp`, backed by `windows/runner/angle_surface_manager.{h,cc}`): mpv renders through its OpenGL render API into a D3D11 texture via ANGLE, which is handed to Flutter as a real `FlutterDesktopGpuSurfaceTexture` (`kFlutterDesktopGpuSurfaceTypeDxgiSharedHandle`) -- staying entirely inside Flutter's own proven texture compositing instead of a sibling window, which is what made the native-child-window approach a dead end. Ported from the (fully removed from this project) `media_kit_video` package's own Windows implementation, whose C++ source remained available in the local pub cache as a concrete, previously-shipping reference even after removal -- not a blind reimplementation from API docs alone. `TryLoadGpuTexture` performs its own **runtime** capability check (ANGLE/D3D11 device creation, and whether this mpv build accepts the OpenGL render API) and falls through to the proven SW pixel-buffer path on a fresh mpv handle if either is unavailable, since GPU/driver support cannot be known ahead of time on unseen hardware -- nothing is committed (no dangling mpv handle) before that fallback decision is made. OS-level HDR and refresh-rate switching (`display_mode_manager.cpp/h`) now runs on this GPU-texture path the same way it previously only ran on the disabled native-window path. **Confirmed working on real Windows hardware over VNC**: Live TV and VOD playback both work, and GPU-accelerated rendering delivers a dramatic, immediately-noticeable latency improvement over the SW pixel-buffer path (the CPU-side render-copy-upload round trip the SW path required every frame is now gone). HDR toggle behavior specifically has not yet been separately verified against an HDR10 source/display. | Blocked for public release on signing and packaging evidence, not on Windows playback or GPU rendering -- both are now confirmed working on real hardware, with a confirmed latency improvement over the prior SW-only path. HDR-specific behavior (as opposed to GPU rendering generally) remains to be verified. |
| macOS desktop | NON-BLOCKING/GATED. macOS runner with Xcode command-line tools, Flutter macOS desktop support, codesign/notarization tooling, and native mpv (`edde746/MPVKit`, `PlaybackBackend.macMpvNative`) as the primary playback path. | Apple Developer ID certificate, notarization credentials, and optional App Store distribution certificate placeholders must be supplied outside git. | macOS signing and App Store notarization are not proven here, but native mpv playback itself is confirmed fully working via hands-on click-testing (video, audio, subtitles, track switching, seeking, clean teardown). `media_kit`/`MediaKitDesktopAdapter` has been removed entirely, including from macOS Multiview, which now also runs on `MacMpvNativeBackend` (`flutter analyze`/`flutter test`/`flutter build macos` all pass; click-tested on real hardware for non-UHD concurrent-tile rendering/audio-focus muting; two concurrent UHD/4K tiles can hit a known VideoToolbox hardware-decode contention limitation, also confirmed on tvOS). | NON-BLOCKING/GATED for this Desktop+Android release. It must not block Linux, Windows, Android, or Android TV release criteria. |
| iOS/iPadOS | NON-BLOCKING/GATED. macOS runner with Xcode, Flutter iOS support, provisioning profiles, device/simulator smoke targets, and native mpv (`edde746/MPVKit`, `PlaybackBackend.appleMpvNative`) as the primary playback path, with `AppleAvKitBackend` as the automatic fallback. | Apple Distribution certificate and provisioning profile placeholders must be supplied by CI secrets or Xcode-managed signing. | iOS release signing and App Review readiness are not proven here, but native mpv playback itself is confirmed fully working, verified today via click-testing on iOS Simulator (iPhone 17 Pro): video, audio, no crash on back navigation. The project's GPL-3.0 relicense removed the prior App Store/GPL blocker for MPVKit. | NON-BLOCKING/GATED for this Desktop+Android release. It must not block Linux, Windows, Android, or Android TV release criteria. |
| tvOS | NON-BLOCKING/GATED. Xcode-managed tvOS runner, Siri Remote/gamepad input, native mpv (`edde746/MPVKit`, `PlaybackBackend.appleMpvNative`) playback path, plugin implementation audit, and device/simulator QA. | Apple tvOS distribution signing placeholders must be supplied outside git. | The pinned Flutter toolchain does not expose an official `flutter build tvos` command; the app instead builds via a manually-wired Xcode project that is already working. Playback is confirmed correct (video, audio, subtitles) via the same native mpv backend as iOS. The overlay-scaling cosmetic bug (playback overlay rendering "boxed in" relative to the video) has been fixed. Store/legal review and App Store submission proof remain open. | NON-BLOCKING/GATED for this Desktop+Android release. Playback itself is no longer blocked; remaining gaps are store/signing/submission evidence and plugin audit. |

## Honest Release Blockers

- Android SDK/JDK setup, signed release artifacts, Play Console metadata, physical Android phone/tablet QA, and physical Android TV hardware validation are not completed by this baseline task. Emulator logs are supplemental only.
- Android now uses native mpv (`PlaybackBackend.androidMpv`, `dev.jdtech.mpv` libmpv bindings, rendered via a `SurfaceView`-hosted `AndroidView`) as the primary playback path, with Media3/ExoPlayer as the automatic fallback and m3u-editor server transcode as the final fallback. Native mpv playback has only been confirmed via limited manual testing on physical Android TV hardware (playback starts, video renders, back-navigation tears down cleanly); subtitle rendering, HDR, and broader device/codec QA remain open before this can be claimed release-ready.
- Windows playback now attempts a GPU path before falling back to the existing SW pixel-buffer texture path: mpv embeds into a native child window via its `wid` option (`windows/runner/desktop_libmpv_backend.cpp`'s `CreateGpuVideoWindow`) and presents its own D3D11 swap chain directly, adapted from the open-source Plezy player's equivalent design (`edde746/plezy`, GPL-3.0). **Open architectural risk, not just an unverified implementation detail:** Plezy's own code comments say this only composites correctly (video behind the Flutter-drawn UI, so player controls stay visible) because their engine build presents Flutter's UI on a topmost DirectComposition visual explicitly ordered above the video window -- a modification to the Flutter engine itself that this project does not have and cannot build here. Flutter's own `platform_view` example ships a Windows target that renders only placeholder text, confirming the stock engine has no platform-view API to lean on instead. On stock Flutter, a plain child window's content is not guaranteed to paint behind what its parent draws in the same region -- ordinary Win32 z-order rules put a child's paint *above* its parent's. Concretely, this may mean video renders on top of player controls rather than behind them, not just "no HDR" or "black screen." Also new: `windows/runner/mpv/display_mode_manager.cpp/h` switches the OS display into HDR mode and matches the display refresh rate to the source's frame rate (at the current resolution only -- no resolution changes are ever attempted) when a GPU-path source is HDR, restoring both on stop/dispose, with a registry-persisted single-record crash-recovery journal (deliberately simplified from Plezy's dual-slot design -- see the header comment in `display_mode_manager.h`) that reapplies the saved original at next launch if a previous run crashed mid-change. This project's own environment has no Windows toolchain at all; the video window, GPU/HDR path, refresh-rate switching, and crash recovery have all now been built and exercised on real Windows hardware, surfacing (and fixing) real bugs a blind implementation pass could not have caught: a 1x1 video window causing every file to fail, a test-suite region collision, and mpv resolving the wrong `libmpv-2.dll` at runtime -- twice. First attempt: the native backend's loader was picking up the `media_kit_libs_windows_video` plugin's bundled build (kept for the MediaKit fallback adapter) ahead of any dedicated build, and that build cannot open a windowed `gpu-next`/D3D11 VO, failing with "Error opening/initializing the selected video_out (--vo) device". Fixed by fetching our own build via CMake `FetchContent` (`windows/CMakeLists.txt`, the same sourceforge `mpv-player-windows` build and version pin the open-source Plezy player fetches) and copying it into the runner output directory as `libmpv-2.dll`, which the loader already tried first -- this was believed sufficient because media_kit's bundled DLL was assumed to be named `mpv-2.dll`. It is not: media_kit_libs_windows_video's own `windows/CMakeLists.txt` names its bundled build `libmpv-2.dll` too (`media_kit_libs_windows_video_bundled_libraries`), so both copies targeted the identical filename in the identical directory, and the fetch fix did nothing to prevent a collision -- it just meant whichever copy step ran later would win. Because `CMAKE_VS_INCLUDE_INSTALL_TO_DEFAULT_BUILD` makes CMake's `install()` step (which copies media_kit's bundled libraries) its own INSTALL project that Visual Studio always builds after every other target -- including the runner target's `POST_BUILD`, which is where the fetched build gets copied -- media_kit's older build always ran last and silently overwrote ours, reproducing the identical "-20"/VO-open failure on real Windows hardware even after the fetch fix. Corrected by renaming our fetched build to `libmpv-gpu-2.dll` on copy (`windows/runner/CMakeLists.txt`) -- a filename nothing else can ever produce, removing the possibility of collision regardless of build order -- and adding it as the first name the loader tries (`kMpvDllNames` in `desktop_libmpv_backend.cpp`), ahead of the `libmpv-2.dll`/`mpv-2.dll` fallbacks kept only for a manually-vendored libmpv. This two-step discovery -- the first fix looked complete and passed a full rebuild, only for a real hardware run to reveal the same symptom via a subtler, order-dependent cause -- is exactly the kind of thing only a real Windows box surfaces, which is why release, not more blind iteration here, is the fastest path to finishing this. Following this discovery, `media_kit`/`media_kit_video`/`media_kit_libs_linux`/`media_kit_libs_windows_video` were removed from this project entirely (not just left unregistered): every platform's playback registration already used only native mpv or the platform's own player as fallback (ExoPlayer on Android, AVKit on Apple), so media_kit was dead weight everywhere, not just a Windows hazard -- it just happened to be Windows where that dead weight actively broke something, twice.

**The z-ordering question is no longer open -- it is now a CONFIRMED BLOCKER.** With the DLL collision fixed, real Windows hardware surfaced two more bugs: (1) mpv's own `ytdl_hook` script failing to find an external youtube-dl/yt-dlp binary (never bundled on any platform, since this app never plays arbitrary web pages) and treating that as a hard load failure -- `libmpv end-file error -17 (ytdl_hook: youtube-dl failed: not found or not enough permissions)` -- even for a plain local-network MP4/MKV file. Fixed by explicitly setting `ytdl=no` on every native mpv backend (`windows/runner/desktop_libmpv_backend.cpp`, `linux/desktop_libmpv_backend.cc`, the three Apple `MpvPlayerCore.swift` cores); Android's Kotlin core already had this set, which is what made it Windows/Linux/Apple-only. (2) Once that was fixed and mpv's D3D11 VO opened successfully (audio plays, no VO-open error), the video area is a **black screen**, not merely "video on top of controls" as the architectural risk above speculated. This is consistent with a second, separate Windows/DXGI quirk layered on top of the z-order problem: a top-level window using a flip-model DXGI swap chain (which the stock Flutter engine does) can cause DWM to stop compositing sibling/child HWND content into the final frame at all, not just reorder it -- the child window's own D3D11 presentation happens, but never reaches the screen. This confirms the native-child-window approach cannot work on stock Flutter, full stop, not just imperfectly: the fix is the fallback design already named above -- render mpv's output into a texture via its OpenGL render API (through ANGLE, the same GL-to-D3D11 translation layer media_kit's own Windows implementation used) and feed it to a `FlutterDesktopGpuSurfaceTexture` (`kFlutterDesktopGpuSurfaceTypeD3d11Texture2D`, a real stock Flutter Windows embedder API -- verified directly against `flutter_texture_registrar.h`/`texture_registrar.h` in the Flutter engine source, not assumed), staying inside Flutter's own proven texture compositing instead of a sibling window. Note this is a different, unrelated fix from the open-source Plezy player's own solution to the same underlying problem: Plezy requires a custom-patched fork of the Flutter engine itself (`edde746/flutter-plezy`, distributed as prebuilt binaries swapped into the local Flutter SDK's engine cache before every build) that adds DirectComposition-visual ordering support Flutter does not expose publicly. This project deliberately does not follow that path -- maintaining a Flutter engine fork across every SDK version bump is a standing engineering commitment with no existing infrastructure here, whereas the texture-based approach stays entirely within normal native-plugin territory using a real, stock, public Flutter API. That rewrite has not been implemented yet. Given that scope, the GPU child-window path was disabled at compile time (`kGpuWindowPathEnabled = false` in `desktop_libmpv_backend.cpp`'s `Load()`) rather than left in a state that silently produces a black screen; every load now goes through the SW pixel-buffer path instead, the same one this project used before GPU/HDR work began.

**Separately, resuming/continue-watching VOD playback was broken on real Windows hardware** (start-from-beginning worked; resuming from a saved position failed every time with a generic "mpv loadfile command failed") -- and turned out to be a real, longstanding bug in mpv's `loadfile` command construction, present identically in both C++ desktop backends (`windows/runner/desktop_libmpv_backend.cpp`, `linux/desktop_libmpv_backend.cc`). mpv's `loadfile` signature is `loadfile <url> [<flags> [<index> [<options>]]]` -- both files built `{"loadfile", url, "replace", "start=90.5", nullptr}`, putting the options string in the *third* positional slot (`<index>`, which mpv expects as an integer playlist-insert position), not the fourth (`<options>`). Some mpv builds tolerate this; whichever one Windows just started fetching does not. The three Apple `MpvPlayerCore.swift` cores and the Android Kotlin core already had this right (`["loadfile", uri, "replace", "0", "start=..."]`, an explicit `"0"` index before the options string) -- this was purely a gap in the two C++ desktop backends, carried over from before this session, not something introduced by this session's other fixes. Fixed by setting `start` via mpv's `set` command (Linux) or as a normal pre-`mpv_initialize` option (Windows, matching how every other option in that function is already set) instead of relying on `loadfile`'s options slot at all, on both platforms.

**Windows GPU-texture rendering rewrite (ANGLE + `FlutterDesktopGpuSurfaceTexture`), confirmed working on real Windows hardware.** With the SW pixel-buffer path proven working (Live TV, VOD from start, VOD resume all confirmed on real Windows hardware), this project implemented the real fix for the native-child-window GPU path's confirmed DWM/flip-model dead end (see above): rendering mpv's output into a D3D11 texture via ANGLE and handing it to Flutter through the real, public `FlutterDesktopGpuSurfaceTexture` embedder API, instead of a sibling window. New files: `windows/runner/angle_surface_manager.h/.cc` (the ANGLE/EGL/D3D11 interop layer -- creates its own D3D11 device and two shared textures, one mpv/ANGLE draws into and one Flutter reads from, copied via `ID3D11DeviceContext::CopyResource` right before Flutter opens the shared handle, to avoid any need to synchronize GL rendering against Flutter's raster thread). `TryLoadGpuTexture` in `desktop_libmpv_backend.cpp` hand-declares mpv's OpenGL render API structs (`mpv_opengl_init_params`, `mpv_opengl_fbo`, `MPV_RENDER_PARAM_OPENGL_INIT_PARAMS`/`_FBO`) the same way the SW path's structs are already hand-declared (this project dlopens libmpv rather than linking `mpv/render.h`), creates an `mpv_render_context` with `MPV_RENDER_API_TYPE_OPENGL` and `get_proc_address = eglGetProcAddress`, and registers a `flutter::GpuSurfaceTexture(kFlutterDesktopGpuSurfaceTypeDxgiSharedHandle, ...)` whose callback renders into the ANGLE surface's FBO 0 and copies it to the published texture before returning -- the same lazy, pull-based-on-raster-thread pattern the existing SW path already uses (`CopyPixels`), just producing a GPU texture descriptor instead of a CPU pixel buffer. `kFlutterDesktopGpuSurfaceTypeDxgiSharedHandle` was chosen deliberately over `kFlutterDesktopGpuSurfaceTypeD3d11Texture2D`: Flutter's own internal ANGLE/D3D11 device (`egl::Manager::GetDevice`) is not reachable through the public embedder API this app links against, ruling out same-device texture sharing -- the shared-handle variant lets the texture be created on any device (this app's own ANGLE-owned one) and opened independently by Flutter's engine.

Ported closely from the (fully removed from this project, see above) `media_kit_video` package's own Windows implementation (`angle_surface_manager.{h,cc}`, `video_output.cc`) -- its C++ source remained on disk in the local pub cache even after the package was removed from `pubspec.yaml`, giving a concrete, previously-shipping reference implementation to adapt rather than a blind reimplementation from ANGLE/EGL API documentation alone. Key adaptations rather than a verbatim port: this project builds with exceptions disabled (`_HAS_EXCEPTIONS=0`), so the reference's `throw std::runtime_error(...)` failure signaling was replaced with an `IsValid()`/bool-return pattern consistent with the rest of this file; dynamic texture resizing was dropped entirely in favor of the same fixed-size (`kTextureWidth`/`kTextureHeight`) approach the SW path already uses, since no other backend in this project has resize plumbing either; and the local mpv option-setting handle is named `gpu_handle` rather than `handle` specifically to keep this function's option calls textually distinct from the SW path's identical-looking ones, for the same reason the (also disabled) native-window path already does this -- this file's own architectural tests scan for exact single occurrences of some option strings.

`TryLoadGpuTexture` performs a genuine runtime capability check, not just a compile-time flag: if `ANGLESurfaceManager` fails to initialize (no suitable GPU/driver, missing ANGLE EGL extension, etc.) or mpv rejects `MPV_RENDER_API_TYPE_OPENGL` outright, nothing has been committed yet (no mpv handle left dangling) and `Load()` falls through to the proven SW path on a fresh handle automatically -- no user-visible failure, no rebuild required, unlike the compile-time-only kill switch the native-window path relies on. A compile-time kill switch (`kGpuTexturePathEnabled`) still exists as a backstop in case the runtime-fallback logic itself has a bug. ANGLE's runtime DLLs (`libEGL.dll`, `libGLESv2.dll`, `d3dcompiler_47.dll`, `zlib.dll`) are now fetched (`windows/CMakeLists.txt`) and bundled next to the executable (`windows/runner/CMakeLists.txt`'s `POST_BUILD` step) the same way `libmpv-gpu-2.dll` already is; ANGLE's own bundled SwiftShader software-Vulkan fallback (`vk_swiftshader.dll`, `vulkan-1.dll`) is deliberately not bundled, since this project already has its own proven SW pixel-buffer fallback for GPU-less machines and would never reach ANGLE's own software path.

**Confirmed on real Windows hardware over VNC on the first real-hardware pass**: Live TV and VOD both play correctly on the new GPU-texture path, with a dramatic, immediately-noticeable latency improvement over the SW pixel-buffer path -- consistent with the ANGLE/D3D11 texture route actually eliminating the CPU-side render-copy-upload round trip the SW path required every frame. This project's own environment still has no Windows toolchain at all, so this result came entirely from the user's own hardware test, the same verification path every other Windows GPU/HDR fix in this document has relied on. Not yet separately verified: HDR toggle behavior against an actual HDR10 source/display (GPU rendering working does not by itself confirm the HDR metadata/OS-display-switch path is correct), and the runtime ANGLE/D3D11-unavailable fallback-to-SW path (not exercised, since ANGLE initialized successfully on the hardware tested).

A related, still-open gap surfaced by this same investigation: Windows/Linux desktop has no server-transcode fallback adapter registered at all (`buildPlaybackOrchestrator()` in `app_router.dart` registers only `desktopLibmpv`/`macMpvNative` for desktop), so any mpv load failure on desktop -- VOD resume or otherwise -- is currently a dead end with no automatic recovery, unlike Android/Apple's ExoPlayer/AVKit fallbacks. Also fixed in the same pass: `PlayerScreen._openAndSeek` (`lib/features/player/player_screen.dart`) called `orchestrator.seek()` immediately after `open()` regardless of whether `open()` actually left an active adapter behind; on a non-recoverable failure this threw a second, unrelated `StateError` ("PlaybackOrchestrator has no active backend") that overwrote the real error message already surfaced through the proper error stream, which is what actually made this bug hard to diagnose from the UI alone.
- Linux production packaging must bundle `libmpv.so.2`, verify dynamic dependencies and ZIP integrity, and include license notices before public release.
- Linux Wayland sessions use a GPU-rendered video plane (`linux/wayland_video_surface.h/.cc`, a `wl_subsurface` stacked below the Flutter window driven by mpv's OpenGL render API through its own isolated EGL context -- Flutter's Linux embedder has no platform-view API and its `FlTextureGL` is hardcoded to 8-bit RGBA, so this is the only route to GPU-accelerated rendering there) before falling back to the original software pixel-buffer texture path. This was built and click-tested on real Wayland hardware over a VNC session (playback, scrubbing, and multiview all confirmed working; `M3U_TV_SHOW_PLAYBACK_DIAGNOSTICS=true` confirms the "Desktop libmpv" backend is active). Two CMake build issues found on that hardware (`PkgConfig::WAYLAND_CLIENT` target not found; a required `IMPORTED_TARGET` keyword missing on the Wayland `pkg_check_modules` calls) have been fixed.
- The GPU video plane now additionally attempts HDR passthrough via Wayland's `wp_color_manager_v1` (`color-management-v1`) protocol (`linux/hdr_metadata.h`, `linux/wayland/color-management-v1-client-protocol.h/.c` vendored from upstream wayland-protocols, and the color-management additions to `linux/wayland_video_surface.h/.cc`): a two-phase image-description transition (stage and validate against the compositor, switch mpv's output color space once it settles, then commit and present atomically), ported closely from the open-source Plezy player's equivalent implementation (`edde746/plezy`, GPL-3.0) as a reference, with its async multi-request toggle-queue/generation-counter machinery deliberately left out -- this app applies HDR automatically per source rather than exposing a user toggle, so a single serialized transition per `video-params` change is equivalent and far simpler. One CMake build issue found on real hardware (`CMAKE_C_COMPILE_OBJECT` not set -- the vendored protocol file is plain C, and `project()` only declared the `CXX` language) has been fixed (`project(runner LANGUAGES CXX C)`); the build now succeeds and click-testing confirmed playback still works with the HDR code paths compiled in. The HDR passthrough behavior itself (an actual HDR file, an HDR-capable display switched into HDR, a compositor implementing `color-management-v1` such as KWin 6.4+) has not yet been exercised in this project's own testing, but the design and protocol usage follow Plezy's shipping implementation closely -- this project does not have the range of HDR displays and compositors needed to self-verify further, and getting that coverage is exactly what shipping this to users on their own hardware is for. X11 sessions are unaffected either way (no GPU path, no HDR).
- Windows production packaging must verify `libmpv-gpu-2.dll` and required runtime files inside the ZIP. Authenticode or MSIX signing and real-device playback evidence remain release blockers.
- Apple/tvOS gates do not block the Desktop+Android release track. Apple work is a non-blocking gated track while Linux desktop, Windows desktop, Android phone/tablet, and Android TV remain the blocking release targets.
- macOS and iOS/iPadOS require Xcode host evidence, provisioning/codesigning, and notarization or App Store review preparation before any Apple release claim. Native mpv (MPVKit) playback itself is confirmed working on both platforms; what remains is submission/signing evidence, not the playback backend.
- MPVKit/libmpv is now the primary shipping playback backend on iOS/tvOS/macOS: the project's GPL-3.0 relicense removed the prior App Store/GPL incompatibility. A dedicated legal/App Store review pass on GPL-3.0 binary distribution obligations (source offers, review guideline 2.5.1) has not yet been done and should happen before submission. macOS Multiview now runs on native mpv (`MacMpvNativeBackend`) instead of media_kit; not yet click-tested on real hardware (see the macOS desktop row above).
- tvOS build and playback both work via a manually-wired Xcode project; remaining gaps before a production claim are missing tvOS plugin implementation audit, Platform.isIOS vs tvOS platform detection risks, and community/custom-embedder-equivalent proof for store submission. The overlay-scaling cosmetic bug has been fixed.
- No signing keys, store credentials, private stream URLs, or provider credentials are stored in this repository.


## Android App ID, Signing, and TV Release Metadata

- Application ID: `dev.sparkison.tv`; active Android release configuration must not use `com.example` or other template identifiers.
- `flutter_client/android/app/build.gradle.kts` reads `ANDROID_KEYSTORE_PATH`, `ANDROID_KEY_ALIAS`, `ANDROID_KEYSTORE_PASSWORD`, and `ANDROID_KEY_PASSWORD` from Gradle properties, environment variables, or the local ignored file `flutter_client/android/signing.properties`.
- The publication workflow enables `ANDROID_REQUIRE_RELEASE_SIGNING`. Missing signing values or a missing keystore path therefore fail publication instead of producing a debug-signed artifact. The workflow verifies the APK signer before staging and records its SHA-256 certificate digest in the job summary.
- Without `ANDROID_REQUIRE_RELEASE_SIGNING`, contributors can create a debug-signed release build for local development only and must not publish it.
- Users upgrading from a debug-signed version must uninstall the existing debug-signed app once before installing the first stable release-signed build. Uninstalling removes local app data and settings, so users must configure the app again afterward.
- Android TV launcher metadata must remain present in `flutter_client/android/app/src/main/AndroidManifest.xml`: `android.software.leanback`, optional touchscreen support, application banner, exported main activity, and `LEANBACK_LAUNCHER` category.
- Play Store and Android TV store distribution remain blocked until signed AAB/APK artifacts, Play Console metadata, data-safety declarations, codec/legal review, physical Android phone/tablet QA, and physical Android TV hardware QA are supplied as release evidence outside git.

## Dependency and License Gates

- Media3/ExoPlayer is the Android playback dependency path for this release and must keep its AndroidX license notices with generated or bundled notices for distributed artifacts.
- Flutter plugins and Flutter SDK notices must be included in each store or sideload artifact through the platform's generated license notice mechanism or an equivalent third-party notices file.
- mpv/libmpv, FFmpeg, MPVKit, and libass are desktop/native playback dependencies only where explicitly implemented and packaged. Their LGPL/GPL build flags, dynamic/static linking choices, source-offer obligations, App Store policy impact, native dependency review, and attribution notices must be reviewed before public distribution.
- ANGLE (Windows GPU-texture rendering path, `windows/runner/angle_surface_manager.{h,cc}`) is BSD-licensed (Google); the pre-packaged binaries this project fetches (`alexmercerind/flutter-windows-ANGLE-OpenGL-ES` release `v1.0.1`) bundle SwiftShader (Apache-2.0) components as well, though this project only bundles ANGLE's own D3D11-backed DLLs, not SwiftShader's software-Vulkan fallback (`vk_swiftshader.dll`/`vulkan-1.dll`). Attribution notices for both remain to be reviewed before public distribution, same as the other native dependencies above.
- GPL policy: do not ship GPL-only binaries, GPL-derived code, or Plezy reference code in a store/direct-download artifact unless the release owner explicitly accepts the GPL distribution obligations and records that decision in release evidence.
- License notices are a release gate. No Play Store, App Store, Microsoft Store, sideload, or direct-download release is complete until dependency/license notices and codec redistribution obligations are reviewed and saved with the signed artifact evidence.
