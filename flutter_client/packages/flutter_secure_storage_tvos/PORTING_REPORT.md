# flutter_secure_storage_tvos — porting report

Generated by `flutter-tvos plugin port` on 2026-06-17.

Source: `flutter_secure_storage_darwin` 0.4.0 (path: `/Users/shaunparkison/.pub-cache/hosted/pub.dev/flutter_secure_storage_darwin-0.4.0`)
Base platform: ios (Swift)
Output: `./flutter_secure_storage_tvos`

> ## ⚠️ Partial tvOS port
>
> This plugin uses LocalAuthentication at type / top-level scope — APIs that **do not exist on tvOS**. The porter could not stub these behind a method channel, so it wrapped each one’s enclosing declaration in `#if !os(tvOS)` / `#if !TARGET_OS_TV`. The package still compiles on tvOS with **those features disabled**; everything else is ported normally. Each disabled region is listed under “Disabled on tvOS” below so you can hand-port it if tvOS needs that capability. A symbol used from many places may still need manual cleanup — verify the build.

## Summary

| Status | Count |
|---|---|
| Methods ported as-is | 20 |
| Methods stubbed (iOS-only) | 0 |
| Native regions disabled on tvOS | 2 |
| tvOS build outlook | ⚠️ partial — 2 region(s) disabled; verify the build |
| Manual review items | 2 |

## Methods

### `and` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `applicationPassword` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `biometryAny` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `biometryCurrentSet` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `containsKey` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `delete` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `deleteAll` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `devicePasscode` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `first_unlock` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `first_unlock_this_device` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `isProtectedDataAvailable` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `or` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `passcode` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `privateKeyUsage` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `read` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `readAll` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `unlocked` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `unlocked_this_device` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `userPresence` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

### `write` ✓ ported

No tvOS-incompatible APIs detected in this handler. Copied across unchanged.

## Imports removed

- `import LocalAuthentication` (`tvos/Classes/FlutterSecureStorageDarwinPlugin.swift:5`)
- `import LocalAuthentication` (`tvos/Classes/FlutterSecureStorage.swift:11`)

## Cross-platform Dart pruned

None. The source ships no Dart files for non-Apple platforms — nothing had to be removed.

## Disabled on tvOS

Each enclosing declaration below was wrapped in `#if !os(tvOS)` / `#if !TARGET_OS_TV` so the package builds on tvOS **without that feature**. To support it on tvOS, replace the disabled code with a tvOS-capable implementation (or accept the feature is unavailable on tvOS).

| API | Where | Matched | Why it cannot run on tvOS |
|---|---|---|---|
| LocalAuthentication | `tvos/Classes/FlutterSecureStorageDarwinPlugin.swift:197` | `LAContext` | tvOS has no biometric authentication. Use a remote-pairing flow if you need user identity (e.g. a sign-in code displayed on the TV that the user enters in a phone app). |
| LocalAuthentication | `tvos/Classes/FlutterSecureStorage.swift:67` | `LAContext` | tvOS has no biometric authentication. Use a remote-pairing flow if you need user identity (e.g. a sign-in code displayed on the TV that the user enters in a phone app). |

## Manual review items

1. `tvos/Classes/FlutterSecureStorageDarwinPlugin.swift:197` — LocalAuthentication disabled on tvOS (enclosing declaration compiled out behind `#if !os(tvOS)`/`#if !TARGET_OS_TV`). Hand-port if tvOS needs this capability.
2. `tvos/Classes/FlutterSecureStorage.swift:67` — LocalAuthentication disabled on tvOS (enclosing declaration compiled out behind `#if !os(tvOS)`/`#if !TARGET_OS_TV`). Hand-port if tvOS needs this capability.

## Checklist

- [ ] Read every `✗ stubbed` method above and confirm returning `FlutterMethodNotImplemented` is acceptable on tvOS.
- [ ] Review every `⚠️ partial` method against a real Apple TV (behaviour differs from iOS).
- [ ] Confirm the removed imports were not load-bearing for still-supported code paths.
- [ ] `flutter-tvos build tvos --simulator --debug` from the plugin's example app compiles the generated registrant.
- [ ] Bump the version and update `CHANGELOG.md` before publishing.

---

Manual review required. Read this report top-to-bottom before publishing `flutter_secure_storage_tvos`.
